# FlashCart — Backend (Django)

Features:
- Django + DRF + SimpleJWT
- PostgreSQL
- Redis (cache + celery broker + channels)
- Celery (worker + beat)
- Channels (WebSockets)
- Docker + docker-compose
- Ready for Railway deployment

